{% extends 'base.html.twig' %}

{% block title %}Mes présentations{% endblock %}



{% block body %}

{% include 'partial/header.html.twig' %}


   <div class="dashboard">
        <div class="dashboard-toolbar">
            <h1>Mes présentations</h1>
            <a href="{{ path('app_presentation_new') }}" class="btn btn-primary">
                Nouvelle présentation
            </a>
        </div>
        <div class="presentation-grid">
            {% for presentation in presentations %}
                <article class="presentation-card">
                    <div
                        class="presentation-card-thumb"
                        style="background-color: {{ presentation.backgroundColor }}"
                    >
                        {% if presentation.imageFilename %}
                            <img
                                src="{{ asset('uploads/' ~ presentation.imageFilename) }}"
                                alt="{{ presentation.title }}"
                            >
                        {% endif %}
                    </div>
                    <div class="presentation-card-meta">
                        <h2>{{ presentation.title }}</h2>
                        <time>Modifié le {{ presentation.createdAt|date('d/m/Y') }}</time>
                    </div>
                    <a
                        href="{{ path('app_presentation_edit', { id: presentation.id }) }}"
                        class="btn btn-secondary"
                    >
                        Modifier
                    </a>
                </article>
            {% else %}
                <p class="dashboard-empty">Aucune présentation pour le moment.</p>
            {% endfor %}
        </div>
    </div>
{% endblock %}
